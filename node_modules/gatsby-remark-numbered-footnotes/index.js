"use strict";

var footnotes = require(`remark-numbered-footnotes`);

module.exports = function (_ref) {
  var markdownAST = _ref.markdownAST;
  return new Promise(function (resolve) {
    footnotes()(markdownAST);

    return resolve(markdownAST);
  });
};